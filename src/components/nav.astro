---
import { Icon } from 'astro-icon/components';
import ThemeToggle from './theme-toggle.astro';

interface NavItem {
  label: string;
  href: string;
  icon: string;
}

const navItems: NavItem[] = [
  {
    label: "Home",
    href: "#home",
    icon: "mdi:home",
  },
  {
    label: "Career",
    href: "#career",
    icon: "mdi:work",
  },
  {
    label: "Projects",
    href: "#projects",
    icon: "mdi:computer",
  },
  {
    label: "Tech",
    href: "#tech",
    icon: "mdi:code-tags",
  },
  {
    label: "Contact",
    href: "#contact",
    icon: "mdi:message",
  },
];
---

<header class="fixed bottom-4 md:bottom-auto md:top-5 left-1/2 -translate-x-1/2 z-50 w-[94%] sm:w-auto max-w-fit pointer-events-none reveal-header">
  <nav
    id="main-nav"
    aria-label="Main Navigation"
    class="pointer-events-auto flex items-center justify-between sm:justify-center gap-1.5 sm:gap-3 md:gap-4 px-2.5 sm:px-4 py-1.5 sm:py-2 rounded-full bg-card/85 border border-card-border backdrop-blur-xl shadow-[0_8px_32px_rgba(0,0,0,0.18)] transition-all duration-300 hover:border-accent/30"
  >
    <!-- Brand Monogram -->
    <a
      href="#home"
      class="flex items-center gap-2 group focus:outline-none focus-visible:ring-2 focus-visible:ring-accent rounded-full shrink-0"
      aria-label="Yonas Girma - Home"
    >
      <div class="w-8 h-8 rounded-full bg-accent/15 border border-accent/30 text-accent font-heading font-bold text-xs sm:text-sm flex items-center justify-center group-hover:scale-105 group-hover:bg-accent group-hover:text-background transition-all duration-300 shadow-sm">
        YG
      </div>
      <span class="font-heading font-semibold text-maintext text-sm hidden lg:inline tracking-tight group-hover:text-accent transition-colors">
        Yonas<span class="text-accent">.</span>
      </span>
    </a>

    <!-- Divider -->
    <div class="h-4 w-px bg-card-border shrink-0 hidden sm:block"></div>

    <!-- Navigation Links -->
    <ul class="flex items-center gap-1 sm:gap-1.5 md:gap-2">
      {
        navItems.map((item) => (
          <li>
            <a
              href={item.href}
              class="nav-link relative px-2.5 sm:px-3 py-1.5 rounded-full text-xs sm:text-sm font-body font-medium text-subtext hover:text-maintext transition-all duration-200 flex items-center gap-1.5 group focus:outline-none focus-visible:ring-2 focus-visible:ring-accent"
            >
              <Icon 
                name={item.icon} 
                class="w-3.5 h-3.5 sm:w-4 sm:h-4 text-subtext/75 group-hover:text-accent transition-colors md:hidden" 
              />
              <span class="hidden md:inline">{item.label}</span>
              <span class="md:hidden text-[11px]">{item.label}</span>
            </a>
          </li>
        ))
      }
    </ul>

    <!-- Divider -->
    <div class="h-4 w-px bg-card-border shrink-0"></div>

    <!-- Theme Toggle -->
    <div class="shrink-0 flex items-center">
      <ThemeToggle />
    </div>
  </nav>
</header>

<style>
  .reveal-header {
    opacity: 0;
    transform: translate(-50%, 15px);
    animation: revealHeaderIn 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }

  @keyframes revealHeaderIn {
    to {
      opacity: 1;
      transform: translate(-50%, 0);
    }
  }

  .nav-link {
    transition: color 0.25s ease, background-color 0.25s ease, transform 0.25s ease;
  }

  .nav-link:hover:not(.active) {
    background-color: rgba(var(--color-accent-rgb), 0.08);
    color: var(--color-maintext);
  }

  .nav-link.active {
    color: var(--color-accent) !important;
    background-color: rgba(var(--color-accent-rgb), 0.15);
    font-weight: 600;
  }

  .nav-link.active svg {
    color: var(--color-accent) !important;
  }

  #main-nav.is-scrolled {
    border-color: rgba(var(--color-accent-rgb), 0.35);
    box-shadow: 0 12px 40px -10px rgba(0, 0, 0, 0.4), 0 0 24px -5px rgba(var(--color-accent-rgb), 0.2);
  }

  @media (prefers-reduced-motion: reduce) {
    .reveal-header {
      animation: none;
      opacity: 1;
      transform: translate(-50%, 0);
    }
  }
</style>

<script>
  function setupNavigation() {
    const nav = document.getElementById("main-nav");
    const sections = document.querySelectorAll("section[id]");
    const navLinks = document.querySelectorAll<HTMLAnchorElement>(".nav-link");

    // Scroll styling enhancement (No layout reflow, pure class toggle)
    let isScrolled = false;
    function handleScroll() {
      const scrolled = window.scrollY > 40;
      if (scrolled !== isScrolled) {
        isScrolled = scrolled;
        nav?.classList.toggle("is-scrolled", isScrolled);
      }
    }
    window.addEventListener("scroll", handleScroll, { passive: true });
    handleScroll();

    // Smooth section scrolling with active update
    document.querySelectorAll<HTMLAnchorElement>('a[href^="#"]').forEach((anchor) => {
      anchor.addEventListener("click", function (e) {
        const target = e.currentTarget as HTMLAnchorElement;
        const targetId = target.getAttribute("href")?.substring(1);
        if (!targetId) return;

        const targetElement = document.getElementById(targetId);
        if (targetElement) {
          e.preventDefault();
          targetElement.scrollIntoView({
            behavior: "smooth",
          });
          
          if (history.pushState) {
            history.pushState(null, '', `#${targetId}`);
          }

          navLinks.forEach((link) => {
            if (link.getAttribute("href") === `#${targetId}`) {
              link.classList.add("active");
            } else {
              link.classList.remove("active");
            }
          });
        }
      });
    });

    // Rock-solid IntersectionObserver for Section Tracking
    // rootMargin: "-20% 0px -60% 0px" triggers when the top of the section enters the upper-middle viewport
    const observerOptions: IntersectionObserverInit = {
      rootMargin: "-20% 0px -60% 0px",
      threshold: 0,
    };

    const observerCallback: IntersectionObserverCallback = (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          const id = entry.target.getAttribute("id");
          navLinks.forEach((link) => {
            const href = link.getAttribute("href");
            if (href === `#${id}`) {
              link.classList.add("active");
            } else {
              link.classList.remove("active");
            }
          });
        }
      });
    };

    const observer = new IntersectionObserver(observerCallback, observerOptions);
    sections.forEach((section) => observer.observe(section));

    // Default to #home if near top
    if (window.scrollY < 120) {
      const homeLink = document.querySelector('.nav-link[href="#home"]');
      if (homeLink) homeLink.classList.add("active");
    }
  }

  setupNavigation();
  document.addEventListener("astro:after-swap", setupNavigation);
</script>
